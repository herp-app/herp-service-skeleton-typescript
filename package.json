{
    "name": "herp-service-skeleton-typescript",
    "version": "1.0.0",
    "description": "A skeleton in typescript to create a herp service",
    "main": "index.js",
    "scripts": {
        "start": "nodemon",
        "build": "rimraf ./build && tsc && cp -R ./src/configuration ./build/configuration",
        "start:build": "npm run build && node build/index.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Johannes Hoffmann",
    "license": "",
    "devDependencies": {
        "@types/express": "^4.17.8",
        "@types/nconf": "^0.10.0",
        "@types/node": "^14.11.8",
        "nodemon": "^2.0.4",
        "rimraf": "^3.0.2",
        "ts-node": "^9.0.0",
        "typescript": "^4.0.3"
    },
    "dependencies": {
        "axios": "^0.20.0",
        "express": "^4.17.1",
        "nconf": "^0.10.0"
    }
}
